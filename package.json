{
  "scripts": {
    "compile": "tsc",
    "coverage": "vitest --coverage",
    "dev": "tsc --watch",
    "test": "vitest"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^1.0.2",
    "typescript": "^5.3.3",
    "vitest": "^1.0.2"
  }
}
